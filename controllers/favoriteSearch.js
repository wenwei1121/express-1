exports.useFavoriteSearch = async (req, res) => {
    const { moduleName } = req.body
    if (moduleName === "readFavoriteSearch") {

    }

    if (moduleName === "addFavoriteSearch") {

    }

    if (moduleName === "updateFavoriteSearch") {
        
    }

    if (moduleName === "deleteFavoriteSearch") {
        
    }
}